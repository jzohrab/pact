[Pact]
LookupModule = pact.plugins.lookup.sample

# Not using Vosk for unit tests, because it runs on another thread,
# which doesn't work well with the unit testing.
VoskModel = 

[Anki]
Ankiconnect = http://localhost:8765/
MediaFolder = test/exported-media

Deck = Testing
NoteType = Audio_clip
AudioField = Audio
TranscriptionField = Text
